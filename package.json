{
  "name": "bible_api",
  "version": "1.0.0",
  "description": "Connect to an API and get some data",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.3"
  }
}
